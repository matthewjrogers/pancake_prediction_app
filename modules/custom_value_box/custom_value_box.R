custom_value_box <- function(input,
                             output,
                             session,
                             color,
                             box_title,
                             box_text,
                             box_icon
                             ){}